.venv/
__pycache__/
*.pyc
.DS_Store

# runtime data (generated, not source)
data/sepa.db
data/sepa.db-shm
data/sepa.db-wal
data/logs/sepa.log
data/logs/sepa.log.*
data/charts/
# data/logs/ — tracked intentionally; each run commits its own folder
data/state.json

# local Claude Code memory / session files
.claude/

# credentials — never commit
.env
*.env
